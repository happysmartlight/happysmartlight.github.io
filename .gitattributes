# Media assets ported from the Jekyll site are tracked via Git LFS to keep the
# repository lean. CI must checkout with lfs:true so real bytes land in dist/.
public/img/** filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
